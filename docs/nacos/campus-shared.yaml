# Nacos 配置中心共享配置 (dataId=campus-shared.yaml, group=DEFAULT_GROUP)
# campus-user 与 campus-gateway 通过 spring.config.import 导入，共享同一份 JWT 配置。
# 推送命令见 docs/nacos/README.md。
jwt:
  secret: campus-trade-jwt-secret-key-change-me-in-production-0123456789
  expiration: 86400000
