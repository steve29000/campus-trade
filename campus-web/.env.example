# 前后端联调开关。
# 设置 VITE_API_BASE 后，已支持的域（登录/注册/商品浏览/发布/改状态）走真实后端，
# 其余域（收藏/分类/认证/聊天/AI/后台）仍走内存 mock。
# 不设置（默认）则整个应用走 mock，可独立运行。
#
# 复制为 .env.local 并取消注释其中一行。
#
# 推荐：经 dev server 的 /api 代理（vite.config.ts 已内置），浏览器同源，绕开网关 CORS：
# VITE_API_BASE=/api
#
# 备选：直连网关（需要网关放行前端 origin 的 CORS）：
# VITE_API_BASE=http://localhost:8080
